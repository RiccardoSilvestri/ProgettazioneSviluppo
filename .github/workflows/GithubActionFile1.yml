name: learn-github-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: [push]
jobs:
  action-environment-debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
    # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      - run: npm install --global surge
      - run: surge --version
      - run: date >> README.md

      - run: npm install
        working-directory: ./web-fronted/
      - run: npm run build
        working-directory: ./web-fronted/
      - run: surge deploy
        working-directory: ./web-fronted/

      - run: | 
          export SURGE_TOKEN=5fdd714872b47ea67d92c8775cbe6f05
          surge ./build/ receptive-mint.surge.sh
      - name: Test run
        run: | 
          date
          ls -la
          pwd 
          echo "CIAO---------------------------------"
          env
      - run: echo "Fineeeeeeeeeeeeeeeeeeeeeeeeeeee"